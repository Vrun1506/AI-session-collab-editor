{
  "name": "mpa-vscode",
  "displayName": "Multiplayer Agent",
  "description": "Share one live agent session with your team — powered by Claude",
  "version": "0.0.1",
  "publisher": "mpa-dev",
  "private": true,
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./out/extension.js",
  "activationEvents": [],
  "contributes": {
    "commands": [
      {
        "command": "mpa.hostSession",
        "title": "Multiplayer Agent: Host Session",
        "category": "Multiplayer Agent"
      },
      {
        "command": "mpa.joinSession",
        "title": "Multiplayer Agent: Join Session",
        "category": "Multiplayer Agent"
      },
      {
        "command": "mpa.interrupt",
        "title": "Multiplayer Agent: Interrupt Turn",
        "category": "Multiplayer Agent"
      },
      {
        "command": "mpa.showPanel",
        "title": "Multiplayer Agent: Show Panel",
        "category": "Multiplayer Agent"
      },
      {
        "command": "mpa.requestDriver",
        "title": "Multiplayer Agent: Ask to Drive",
        "category": "Multiplayer Agent"
      },
      {
        "command": "mpa.releaseDriver",
        "title": "Multiplayer Agent: Release the Wheel",
        "category": "Multiplayer Agent"
      },
      {
        "command": "mpa.rewind",
        "title": "Multiplayer Agent: Rewind to a Checkpoint",
        "category": "Multiplayer Agent"
      },
      {
        "command": "mpa.forkSession",
        "title": "Multiplayer Agent: Fork Session into a New Room",
        "category": "Multiplayer Agent"
      },
      {
        "command": "mpa.exportAudit",
        "title": "Multiplayer Agent: Export Audit Log",
        "category": "Multiplayer Agent"
      },
      {
        "command": "mpa.stopAgent",
        "title": "Multiplayer Agent: Stop Shared Agent (ends it for everyone)",
        "category": "Multiplayer Agent"
      }
    ],
    "configuration": {
      "title": "Multiplayer Agent",
      "properties": {
        "mpa.relayUrl": {
          "type": "string",
          "default": "ws://127.0.0.1:7331",
          "description": "WebSocket URL of the sync-server relay."
        },
        "mpa.displayName": {
          "type": "string",
          "default": "",
          "description": "Name shown to other participants. You are asked for one the first time you join; it is saved per workspace so two windows can be two people."
        },
        "mpa.agentHostEntry": {
          "type": "string",
          "default": "",
          "description": "Absolute path to agent-host dist/index.js. Leave empty to use the sibling package in this monorepo."
        },
        "mpa.relayEntry": {
          "type": "string",
          "default": "",
          "description": "Absolute path to sync-server dist/index.js. The relay is started automatically when nothing is listening on mpa.relayUrl."
        },
        "mpa.token": {
          "type": "string",
          "default": "",
          "description": "Token for a relay on another machine. Leave empty when the relay runs locally — its token is read automatically from ~/.multiplayer-agent/relay-token."
        },
        "mpa.dbPath": {
          "type": "string",
          "default": "",
          "description": "Where an auto-started relay keeps its session log. Defaults to mpa-sessions.db beside the relay."
        },
        "mpa.allowedTools": {
          "type": "string",
          "default": "Read,Glob,Grep",
          "description": "Comma-separated tools the shared agent may run without asking. Everything else goes to the room for approval."
        },
        "mpa.sharedBuffers": {
          "type": "boolean",
          "default": true,
          "description": "Share open files as live documents, so several people and the agent can edit one file at once and agent writes merge into your buffer instead of reloading it. Turn off to fall back to disk writes, where the relay refuses any write to a file someone has unsaved changes in."
        },
        "mpa.disallowedTools": {
          "type": "string",
          "default": "",
          "description": "Tools nobody may approve, however much they want to. Empty by default: every tool outside mpa.allowedTools already needs a human decision, and the relay separately refuses writes to files someone is still editing."
        }
      }
    }
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.test.json && node scripts/prep-tests.mjs && node --test \"dist/**/*.test.js\""
  },
  "dependencies": {
    "@mpa/crdt": "workspace:*",
    "@mpa/protocol": "workspace:*",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/vscode": "^1.90.0",
    "@types/ws": "^8.5.13",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.24.2"
  }
}
